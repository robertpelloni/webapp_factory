# TODO
- [ ] Connect Playwright for actual scraping of App Store RSS feeds.
- [ ] Connect Vercel API tokens for project deployment.
- [ ] Finalize the LLM integration in `src/generator/index.js` to replace mock code strings with actual API calls.
- [ ] Add `shadcn/ui` as a standard component library for generated applications.
- [ ] Integrate a real database (PostgreSQL/Supabase) to replace local SQLite when deploying the orchestration server to production.
